name: Deploy REedoc

on:
  push:
    branches: [ master, redoc ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Generate redoc file
        run: |
          npm install redoc-cli
          ./node_modules/.bin/redoc-cli bundle openapi.yaml
      - name: deploy file
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.KEY }}
          source: "redoc-static.html"
          target: "/home/bitmaelum/domains/api-docs.bitmaelum.com/public_html/index.html"
